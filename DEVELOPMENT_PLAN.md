# 反重力飞行对战游戏开发步骤与开发清单

## 1. 项目目标重述

基于 `prd.md`，首个可交付版本应实现一套不依赖重型物理引擎的 3D 反重力飞行核心体验，并把它扩展为可验证对战玩法的游戏原型。核心目标有四个：

1. 做出稳定、可调参的反重力飞行运动学系统。
2. 做出有“丝滑感”和“漂浮感”的飞行操控。
3. 做出即使在高空或纯黑背景下也能感知速度的视觉反馈。
4. 在此基础上接入最小对战闭环，验证游戏可玩性。

## 2. 开发原则

1. 先验证“飞行手感”，再扩展“对战复杂度”。
2. 物理、视觉、战斗逻辑解耦，避免后期难以调参。
3. 所有关键体验参数都必须配置化，包括 `k`、`mass`、最大速度、FOV 范围、滚转补偿强度、漂移阈值。
4. 每个阶段结束都必须有可运行版本和验收项，不能只停留在代码完成。

## 3. 推荐开发阶段

### 阶段 A：技术预研与项目骨架

目标：搭起最小可运行框架，验证渲染技术路线与调试方式。

执行步骤：

1. 确定技术栈。
   - 推荐：`TypeScript + Vite + Three.js(WebGL)` 先做原型。
   - 原因：WebGPU 可作为第二阶段升级项，先用成熟方案验证手感更稳。
2. 初始化项目结构。
   - 划分 `core/physics`、`core/rendering`、`gameplay`、`ui/debug`、`config` 模块。
3. 建立基础渲染循环。
   - 实现固定更新步长或半固定步长。
   - 将物理更新与渲染更新分离。
4. 建立调试面板。
   - 支持实时调 `k`、`mass`、加速度、FOV、阻力、滚转强度。
5. 加入性能采样。
   - 记录单帧物理耗时、渲染耗时、粒子数量、平均 FPS。

交付物：

- 可启动项目
- 基础场景
- 固定 `dt` 更新循环
- 参数调试面板
- 性能监测面板

验收标准：

- 本地可稳定运行
- 支持实时修改关键飞行参数
- 物理更新链路独立于渲染链路

### 阶段 B：飞行器核心运动学系统

目标：完成不依赖物理引擎的飞行状态更新。

执行步骤：

1. 定义飞行器状态结构。
   - `position`
   - `velocity`
   - `acceleration`
   - `orientationQuaternion`
   - `angularVelocity`
2. 实现输入系统。
   - 推进
   - 刹车/减速
   - 偏航
   - 俯仰
   - 横滚
3. 实现指数平滑或临界阻尼响应。
   - 输入不直接改状态，而是先改目标状态。
   - 由平滑器把目标速度、目标角速度过渡到当前状态。
4. 实现反重力悬浮逻辑。
   - 提供垂直于地表法线的平衡力。
   - 叠加机身姿态导致的推进偏移。
5. 实现基础阻力模型。
   - 前向阻力弱于横向阻力。
   - 防止无上限漂移和失控。
6. 建立参数化质量感。
   - `mass` 影响加速、减速和姿态响应。
   - `k` 影响输入跟手度与阻尼感。

交付物：

- 可控制飞行器自由飞行
- 可调平滑响应
- 可调轻重感
- 无物理引擎的状态更新核心

验收标准：

- 停止输入后飞行器持续滑行并缓慢减速
- 修改 `k` 或 `mass` 后手感明显变化
- 逻辑计算可控制在目标预算内

### 阶段 C：丝滑操控与漂移手感

目标：把“能飞”提升到“飞起来舒服”。

执行步骤：

1. 实现自动 Banking。
   - 转向输入映射为目标滚转角。
   - 滚转角随速度和转向强度动态变化。
2. 实现向心力补偿。
   - 转弯时给出与滚转状态匹配的辅助横向力。
   - 避免只看起来倾斜但实际转向发飘。
3. 实现惯性漂移。
   - 允许速度向量与朝向短时不一致。
   - 设置最大偏离角阈值和自动回正机制。
4. 实现姿态回正策略。
   - 松开输入后，横滚和平移偏差逐步回归稳定。
5. 做手感调参轮次。
   - 至少建立三组手感预设：轻型、中型、重型。

交付物：

- 自然侧倾动画
- 可控漂移感
- 预设化飞行参数表

验收标准：

- 转向时机身存在自然滚转
- 漂移不会导致玩家完全失控
- 停止输入后姿态能平稳恢复

### 阶段 D：速度感与空间参照系统

目标：解决高空静止错觉，让“快”被明显感知。

执行步骤：

1. 实现动态 FOV。
   - 根据速度标量映射相机 FOV。
   - 设置最小/最大阈值与平滑过渡。
2. 实现空速线/空间尘埃粒子。
   - 粒子围绕相机生成。
   - 根据飞行方向反向运动并循环复用。
3. 实现边缘参照物系统。
   - 视野边缘生成云团、碎片、极简几何块。
   - 保证高速时有横向掠过参照。
4. 实现速度特效层。
   - 首选径向模糊、屏幕拉伸、轻微色差中的一种或两种组合。
5. 建立视觉开关与质量等级。
   - 便于测试性能和定位体验问题。

交付物：

- 动态 FOV 系统
- 粒子速度参照系统
- 近地感/边缘掠过系统
- 高速视觉特效

验收标准：

- 纯黑背景下依然能感知飞行速度
- 加速时 FOV 和速度特效明显变化
- 高空环境不再产生静止错觉

### 阶段 E：最小对战原型

目标：在成熟飞行手感上接入最小可玩的战斗循环。

执行步骤：

1. 确定核心对战模式。
   - 推荐先做 `1v1 / 2v2 竞技原型`。
2. 实现战斗基础规则。
   - 血量
   - 护盾
   - 击毁
   - 重生
   - 倒计时
   - 胜负判定
3. 实现基础武器系统。
   - 主武器：高速射线或弹丸武器
   - 副武器：带锁定或弱追踪导弹
4. 实现目标锁定与准星反馈。
   - 基于视锥和距离判断可锁定目标。
5. 实现受击反馈。
   - 护盾闪烁
   - 命中特效
   - 音效占位
6. 实现简易 AI 敌机或本地双机测试。
   - 优先保证战斗循环可反复验证。

交付物：

- 可对战的最小场景
- 基础武器系统
- 命中与击毁反馈
- 简单敌机行为或本地测试机制

验收标准：

- 玩家能在飞行中完成追逐、瞄准、射击、击毁闭环
- 战斗系统不破坏飞行手感
- UI 能清楚反馈目标、血量、命中和胜负

### 阶段 F：关卡、环境与战斗验证

目标：让飞行与对战在真实场景里成立。

执行步骤：

1. 搭建战斗地图原型。
   - 高空浮岛
   - 管道结构
   - 能量环
   - 几何障碍群
2. 实现碰撞近似方案。
   - 不引入重型引擎，使用包围球/AABB/简化网格做命中检测。
3. 加入风险/收益地形。
   - 掩体
   - 狭窄通道
   - 高速穿越区域
4. 验证可读性。
   - 高速状态下，目标、障碍、特效不能互相淹没。

交付物：

- 一张可反复测试的战斗地图
- 简化碰撞系统
- 场景导航参照物

验收标准：

- 地图能支撑追逐、规避、绕障、反打
- 高速飞行下仍能识别战场信息

### 阶段 G：性能优化与验收收口

目标：满足 PRD 的性能和体验验收标准。

执行步骤：

1. 优化物理更新。
   - 避免频繁对象创建
   - 复用向量和四元数
   - 缩减每帧数学开销
2. 优化粒子与特效。
   - 使用对象池或 GPU Instancing
   - 按距离和速度动态裁剪
3. 统一调参与预设。
   - 形成可对比的飞行手感配置表。
4. 做验收测试。
   - 性能
   - 手感
   - 速度感
   - 对战可玩性
5. 输出开发文档。
   - 核心参数说明
   - 架构说明
   - 后续扩展点

交付物：

- 优化后的可玩版本
- 参数说明文档
- 验收测试记录

验收标准：

- 逻辑计算时间接近或达到 `1ms` 目标
- 各项 PRD 验收标准可逐条验证

## 4. 按优先级排序的开发清单

### P0：必须先完成

- [ ] 项目初始化与目录结构
- [ ] 固定步长或半固定步长更新循环
- [ ] 飞行器状态模型（位置、速度、加速度、四元数）
- [ ] 输入系统（推进、偏航、俯仰、横滚、刹车）
- [ ] 指数平滑/临界阻尼响应
- [ ] 反重力悬浮与基础推进逻辑
- [ ] 基础阻力与惯性滑行
- [ ] 调试面板与性能采样

### P1：核心体验

- [ ] 自动 Banking 侧倾
- [ ] 向心力补偿
- [ ] 惯性漂移与回正
- [ ] 动态 FOV
- [ ] 空速线/尘埃粒子系统
- [ ] 视野边缘掠过参照物
- [ ] 高速后处理特效

### P2：最小对战闭环

- [ ] 玩家血量/护盾系统
- [ ] 武器系统（主武器/副武器）
- [ ] 命中检测
- [ ] 目标锁定与 HUD
- [ ] 击毁/重生/胜负判定
- [ ] 简单 AI 敌机或双机测试方案

### P3：场景与内容

- [ ] 战斗地图原型
- [ ] 障碍物与掩体
- [ ] 风险区域和高速穿越区域
- [ ] 环境美术占位资源
- [ ] 音效和受击反馈

### P4：优化与发布准备

- [ ] 向量/四元数对象复用
- [ ] 粒子池与实例化优化
- [ ] 参数预设整理
- [ ] 验收测试用例
- [ ] 开发文档与调参说明

## 5. 建议的模块拆分

```text
src/
  core/
    physics/
      flightModel.ts
      integrator.ts
      damping.ts
      antiGravity.ts
    rendering/
      renderer.ts
      cameraRig.ts
      speedEffects.ts
      particles.ts
  gameplay/
    combat/
    weapons/
    targeting/
    ai/
  scene/
    arena/
    obstacles/
  ui/
    hud/
    debug/
  config/
    flightPresets.ts
    gameRules.ts
```

## 6. 每周执行节奏建议

### 第 1 周

- 完成项目初始化、渲染循环、调试面板
- 建立飞行状态结构和基础输入控制
- 跑通最小飞行原型

### 第 2 周

- 完成平滑响应、反重力、阻力与滑行
- 完成 Banking、漂移、回正
- 产出第一版“手感可调”飞行模型

### 第 3 周

- 完成动态 FOV、粒子系统、速度特效
- 解决高空静止错觉
- 做第一次体验验收

### 第 4 周

- 接入最小对战系统
- 完成武器、命中、击毁、HUD
- 建立 AI 靶机或双机对战测试

### 第 5 周

- 搭建地图原型与障碍布局
- 验证追逐、规避、绕障、交火体验
- 做性能优化和参数收口

## 7. 风险与规避

1. 风险：过早做复杂对战，飞行手感未定型。
   - 规避：先冻结飞行核心，再接战斗。
2. 风险：视觉特效很强，但无法提升速度感。
   - 规避：优先做参照物系统，其次才是后处理。
3. 风险：漂移和 Banking 太强，导致操作失控。
   - 规避：所有补偿项都做阈值和预设。
4. 风险：性能浪费在粒子和后处理上。
   - 规避：从一开始就做质量档位与对象池。

## 8. 最终里程碑定义

### M1：可飞

- 玩家可稳定飞行
- 有滑行、转向、侧倾
- 参数可调

### M2：飞得爽

- 速度感明显
- 高空不再静止
- 手感具备轻重差异

### M3：能对战

- 可追逐、锁定、射击、击毁
- HUD 和战斗规则完整

### M4：可验收

- PRD 指标基本达成
- 性能与体验都可复现验证

